#!/bin/bash

function run_titlebot () {
    [ -d $HOME/log ] || mkdir -p $HOME/log;
    [ -x $HOME/bin/titlebot.pl ] || ( echo "Cannot run $HOME/bin/titlebot.pl"; exit 1 );
    nohup $HOME/bin/titlebot.pl 2>&1 > $HOME/log/titlebot.log &
};

run_titlebot &
echo titlebot started.
